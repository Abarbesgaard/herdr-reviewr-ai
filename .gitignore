# Rust
/target/
**/*.rs.bk
Cargo.lock.bak

# macOS
.DS_Store

# editor / local
.idea/
.vscode/
*.swp
*.log

# herdr local state (if any lands here during dev)
/.herdr-state/

# Prebuilt binary dropped by herdr/install.sh on `herdr plugin install`
/bin/

# herdr-prs runtime state: the picker (pick-repos.sh) rewrites this with your
# personal repo list and local clone paths. Machine-specific, not shared.
/herdr-prs/repos.conf
